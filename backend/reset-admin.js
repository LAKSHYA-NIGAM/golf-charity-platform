require('dotenv').config(); const { createClient } = require('@supabase/supabase-js'); const bcrypt = require('bcryptjs'); const sb = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); bcrypt.hash('admin123', 12).then(hash => sb.from('users').update({password_hash: hash}).eq('email', 'admin@greenheart.io').then(res => console.log('Fixed:', res.error ? res.error : 'Success')));
